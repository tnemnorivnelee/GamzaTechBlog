# 🚀 MarkdownViewer 종합 테스트 문서

이 문서는 **MarkdownViewer** 컴포넌트의 모든 기능을 테스트하기 위한 종합 테스트 글입니다.

## 📝 기본 텍스트 서식

### 강조 및 스타일
- **굵은 글씨 (Bold)**
- *기울임 (Italic)*
- ~~취소선 (Strikethrough)~~
- `인라인 코드 (Inline code)`
- ***굵은 기울임 (Bold + Italic)***

### 여러 수준의 헤딩

# H1 - 메인 제목 (2em, 하단 보더)
## H2 - 서브 제목 (1.5em, 하단 보더)
### H3 - 소제목 (1.25em)
#### H4 - 작은 제목 (1em)
##### H5 - 더 작은 제목 (0.875em)
###### H6 - 가장 작은 제목 (0.85em, 회색)

---

## 📋 목록과 체크박스

### 순서 없는 목록
- 첫 번째 항목
- 두 번째 항목
  - 중첩된 항목 1
  - 중첩된 항목 2
    - 더 깊은 중첩
- 세 번째 항목

### 순서 있는 목록
1. 첫 번째 단계
2. 두 번째 단계
   1. 중첩된 단계 A
   2. 중첩된 단계 B
3. 세 번째 단계

### 할 일 목록 (GitHub Flavored Markdown)
- [x] ✅ 완료된 작업
- [x] ✅ 또 다른 완료된 작업
- [ ] ⭐ 진행 중인 작업
- [ ] 📝 예정된 작업
- [ ] 🎯 미래의 계획

---

## 💬 인용구

> 이것은 기본 인용구입니다.
> 
> GitHub 스타일의 회색 보더와 배경을 가집니다.

> 여러 줄로 구성된 인용구도 가능합니다.
> 
> 각 줄은 `>` 기호로 시작합니다.
> 
> > 중첩된 인용구도 지원됩니다.
> > 
> > 이렇게 말이죠!

---

## 🔗 링크와 이미지

### 링크 테스트
- [내부 링크](/)
- [외부 링크 - GitHub](https://github.com) (새 탭에서 열림)
- [외부 링크 - Google](https://google.com)
- 자동 링크: https://www.example.com

### 이미지 테스트
![대체 텍스트](https://via.placeholder.com/600x300/4285f4/ffffff?text=MarkdownViewer+Test+Image)

*위 이미지는 반응형으로 처리되며, 로드 에러 시 안전하게 처리됩니다.*

---

## 💻 코드 블록

### 인라인 코드
JavaScript에서 `console.log('Hello World!')`를 사용하여 출력할 수 있습니다.

### 문법 강조가 적용된 코드 블록

```javascript
// JavaScript 예제
function greet(name) {
  console.log(`Hello, ${name}! 👋`);
  
  const message = {
    text: "Welcome to MarkdownViewer!",
    timestamp: new Date(),
    isActive: true
  };
  
  return message;
}

greet("Developer");
```

```typescript
// TypeScript 예제
interface User {
  id: number;
  name: string;
  email: string;
  isActive?: boolean;
}

const users: User[] = [
  { id: 1, name: "Alice", email: "alice@example.com", isActive: true },
  { id: 2, name: "Bob", email: "bob@example.com" }
];

function findActiveUsers(users: User[]): User[] {
  return users.filter(user => user.isActive ?? false);
}
```

```python
# Python 예제
import datetime
from typing import List, Optional

class BlogPost:
    def __init__(self, title: str, content: str, author: str):
        self.title = title
        self.content = content
        self.author = author
        self.created_at = datetime.datetime.now()
    
    def __str__(self) -> str:
        return f"📝 {self.title} by {self.author}"

# 사용 예제
post = BlogPost(
    title="MarkdownViewer 테스트",
    content="이것은 테스트 포스트입니다!",
    author="개발자"
)
print(post)
```

```sql
-- SQL 예제
SELECT 
  u.id,
  u.name,
  u.email,
  COUNT(p.id) as post_count
FROM users u
LEFT JOIN posts p ON u.id = p.author_id
WHERE u.is_active = true
GROUP BY u.id, u.name, u.email
HAVING COUNT(p.id) > 0
ORDER BY post_count DESC
LIMIT 10;
```

```bash
# Bash 스크립트 예제
#!/bin/bash

echo "🚀 MarkdownViewer 배포 스크립트"

# 의존성 설치
yarn install

# 빌드 실행
yarn build

# 테스트 실행
yarn test

echo "✅ 배포 완료!"
```

---

## 📊 테이블

### 기본 테이블
| 기능 | 지원 여부 | 설명 |
|------|-----------|------|
| 문법 강조 | ✅ | highlight.js 사용 |
| 수학 공식 | ✅ | KaTeX 지원 |
| 이모지 | ✅ | remark-emoji |
| GitHub Flavored | ✅ | 완전 지원 |

### 정렬이 적용된 테이블
| 언어 | 인기도 | 난이도 | 추천도 |
|:-----|-------:|:-----:|:------:|
| JavaScript | 95% | 중간 | ⭐⭐⭐⭐⭐ |
| TypeScript | 85% | 중상 | ⭐⭐⭐⭐⭐ |
| Python | 90% | 쉬움 | ⭐⭐⭐⭐⭐ |
| Rust | 70% | 어려움 | ⭐⭐⭐⭐ |

---

## 🧮 수학 공식 (LaTeX)

### 인라인 수학
유명한 아인슈타인의 질량-에너지 등가식: $E = mc^2$

피타고라스 정리: $a^2 + b^2 = c^2$

### 블록 수학 공식

$$
\int_{-\infty}^{\infty} e^{-x^2} dx = \sqrt{\pi}
$$

$$
\sum_{n=1}^{\infty} \frac{1}{n^2} = \frac{\pi^2}{6}
$$

$$
\begin{align}
\nabla \times \vec{\mathbf{B}} -\, \frac1c\, \frac{\partial\vec{\mathbf{E}}}{\partial t} & = \frac{4\pi}{c}\vec{\mathbf{j}} \\
\nabla \cdot \vec{\mathbf{E}} & = 4 \pi \rho \\
\nabla \times \vec{\mathbf{E}}\, +\, \frac1c\, \frac{\partial\vec{\mathbf{B}}}{\partial t} & = \vec{\mathbf{0}} \\
\nabla \cdot \vec{\mathbf{B}} & = 0
\end{align}
$$

---

## 😄 이모지 테스트

### 텍스트 이모지 변환
- :rocket: → 🚀
- :fire: → 🔥
- :heart: → ❤️
- :star: → ⭐
- :thumbsup: → 👍
- :tada: → 🎉
- :bug: → 🐛
- :hammer: → 🔨
- :sparkles: → ✨
- :books: → 📚

### 감정 표현
- :smile: :joy: :heart_eyes: :thinking: :confused:
- :angry: :cry: :fearful: :sleeping: :dizzy_face:

---

## 🏷️ HTML 태그 지원

<div style="background: linear-gradient(45deg, #ff6b6b, #4ecdc4); padding: 20px; border-radius: 10px; color: white; text-align: center; margin: 20px 0;">
  <h3 style="margin: 0;">🎨 HTML이 지원됩니다!</h3>
  <p style="margin: 10px 0 0 0;">rehype-raw 덕분에 안전하게 HTML 태그를 사용할 수 있습니다.</p>
</div>

<details>
<summary>📖 클릭하여 세부 정보 보기</summary>

이것은 HTML의 `<details>` 태그입니다!

- 접을 수 있는 콘텐츠를 만들 때 유용합니다
- **마크다운**과 `코드`도 함께 사용할 수 있습니다
- 사용자 경험을 향상시킵니다

</details>

---

## 📏 수평선 테스트

위와 아래에 수평선이 있습니다.

---

***

___

---

## 🔄 줄바꿈 테스트

이 문장 뒤에는 단일 줄바꿈이 있습니다.
이 문장은 바로 다음 줄에 나타납니다. (remark-breaks 덕분)

이 문단과

이 문단 사이에는 빈 줄이 있습니다.

## ✅ 테스트 완료!

모든 마크다운 기능이 정상적으로 렌더링되었다면 **MarkdownViewer**가 성공적으로 구현된 것입니다! 🎉

### 지원 기능 체크리스트
- [x] 📝 기본 텍스트 서식 (굵게, 기울임, 취소선)
- [x] 📋 목록 (순서 있음/없음, 중첩, 체크박스)
- [x] 💬 인용구 (단일/중첩)
- [x] 🔗 링크 (내부/외부, 자동 감지)
- [x] 🖼️ 이미지 (반응형, 에러 처리)
- [x] 💻 코드 (인라인, 블록, 문법 강조)
- [x] 📊 테이블 (기본, 정렬)
- [x] 🧮 수학 공식 (인라인, 블록)
- [x] 😄 이모지 (텍스트 → 이모지 변환)
- [x] 🏷️ HTML 태그 (안전한 렌더링)
- [x] 📏 수평선
- [x] 🔄 줄바꿈 (GitHub 스타일)

> 💡 **팁**: 이 문서를 북마크하여 마크다운 문법 참고서로 활용하세요!